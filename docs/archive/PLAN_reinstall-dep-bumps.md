## Title

Clean reinstall — dependency version bumps

## Plan

- [x] Run `#reinstall` (remove node_modules/lockfile/.next, `npm install --legacy-peer-deps`, force fresh `nextjs-shared` git-ref resolution, `tsc --noEmit`, `npm run build`)
- [x] Keep the dependency version bumps npm resolved during the reinstall (confirmed correct by user)

## Changes

### package.json
- `pg` 8.21.0 → 8.23.0
- `react` / `react-dom` 19.2.7 → 19.2.8
- `@tailwindcss/postcss` 4.3.1 → 4.3.3
- `tailwindcss` 4.3.1 → 4.3.3
- `@types/pg` 8.20.0 → 8.23.1
- `@types/react` 19.2.17 → 19.2.18
- `@types/react-dom` 19.2.3 → 19.2.5
- npm re-resolved these fresh maxima after the lockfile was deleted; `save-exact=false` in `.npmrc` let the resolved versions be written back. User confirmed the bumps are correct.

### package-lock.json
- Regenerated to match the reinstalled tree. `nextjs-shared` resolved to v2.1.84 (commit f4f1fafe).

### .npmrc (new, previously untracked)
- `save-exact=false`
- `legacy-peer-deps=true`

### tsconfig.tsbuildinfo
- Incremental TypeScript build cache, regenerated by `tsc`/`next build`.

## Testing

- [ ] App still builds and runs (`npm run locallocal`)
- [ ] Copy Tables and Schema Sync tabs load and function
